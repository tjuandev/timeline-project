import clsx from 'clsx'

export const S = {
  container: clsx(
    'p-10',
    'flex',
    'items-center',
    'justify-center',
    'min-h-screen'
  ),
  timelineContainer: clsx('max-w-7xl', 'w-full')
}
